<div class="layout">
    {% if label %}
        <header class="widget-section-header">
            <h1 class="widget-title">{{ label }}</h1>
            <nav class="widget-links">{{ regions.links }}</nav>
        </header>
    {% endif %}
    <div class="widget-3col-wrapper">
        <div class="widget-left">{{ regions.left }}</div>
        <div class="widget-right-first">{{ regions.right_1 }}</div>
        <div class="widget-right-second">{{ regions.right_2 }}</div>
    </div>
    <div class="clearfix"></div>
</div>
